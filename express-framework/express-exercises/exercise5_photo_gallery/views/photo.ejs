<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= photo.title %></title>
  <style>
    body { font-family: Arial, sans-serif; max-width: 700px; margin: 40px auto; padding: 0 20px; }
    .placeholder { width: 100%; height: 350px; background: #ddd; display: flex; align-items: center; justify-content: center; color: #999; font-size: 18px; border-radius: 8px; }
    a { color: #333; }
  </style>
</head>
<body>
  <a href="/">← Back to Gallery</a>
  <h1><%= photo.title %></h1>
  <div class="placeholder">[ <%= photo.title %> Image ]</div>
  <p><%= photo.description %></p>
</body>
</html>
